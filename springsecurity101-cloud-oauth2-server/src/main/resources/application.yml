server:
  port: 8080

spring:
  application:
    name: oauth2-server
  datasource:
    url: jdbc:mysql://localhost:3306/oauth?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: vincent
    password: 1q2w3e4R